export type ComicType = {
    id: number,
    title: string,
     thumbnail: {
        path: string,
        extension: string,
    },
}

export type cardType = {
    title: string,
    picture: string
}